import TestProvider from "@/tests/TestProvider"

describe("Search Component", () => {
  it("If search field not focused, should hide search container", () => {})
  it("If search field focused, should show container", () => {})
  it("If search by keyword random, return empty data and show not found state (msw)", () => {})
  it("If search by expected keyword, return the data with expected keyword and have same length card and data", () => {})
})
