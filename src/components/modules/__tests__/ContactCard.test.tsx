import TestProvider from "@/tests/TestProvider"

describe("Contact Card", () => {
  it("If props data available, it should be rendered with shows name and phone number", () => {})
  it("If phone number > 1, it shows badge +n more", () => {})
  it("If contact card clicked, it should show modal contact detail", () => {})
  it("If contact card clicked from type “home” it should render button favorite, edit, and delete", () => {})
  it("If user click favorite, it should be rendered toaster", () => {})
  it("If user click edit, it should show contact form edit", () => {})
  it("If user click delete, it should show confirmation box", () => {})
  it("If contact card clicked from type “favorite” it should render button remove from favorite", () => {})
})
