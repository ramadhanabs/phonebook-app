import TestProvider from "@/tests/TestProvider"

describe("Contact List", () => {
  it("If loading/fetching show component loader", () => {})
  it("If contact list 0, show empty state", () => {})
  it("If contact list > 0, show component (msw)", () => {})
  it("If contact list === n, show n card contact (msw)", () => {})
  it("If user mark contact as favorite, it should be removed from contact list", () => {})
})
