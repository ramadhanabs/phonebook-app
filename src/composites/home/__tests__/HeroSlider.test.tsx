import TestProvider from "@/tests/TestProvider"

describe("Favorite Contact Slider", () => {
    it("If data favorite length > 0, component should be rendered", () => {})
    it("If data favorite length === 0, component should be hidden", () => {})
})  